eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
microservice:
  registration-service:
    endpoints:
      endpoint:
        uri: http://localhost:9595/registration/servicetype/VEHICLE-SERVICE
    
